import React, { useState } from 'react';
import {
    Box,
    Drawer,
    List,
    ListItem,
    ListItemText,
    Typography,
    TextField,
    Button,
    MenuItem,
    Grid,
    Paper,
} from '@mui/material';
const AttendancePage: React.FC = () => (
    <Typography variant="h5">Attendance Page</Typography>
);
export default AttendancePage;